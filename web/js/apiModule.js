var apiModule = (function($, Config) {

    /**
     * Init
     *
     * @return {[type]} [description]
     */
    var init = function() {
    };

    return {
        init: init,
    };

})(jQuery, Config);

$(function() {
    apiModule.init();
});
